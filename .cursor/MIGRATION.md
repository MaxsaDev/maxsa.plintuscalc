# Инструкция по миграции эталонных правил

Это руководство описывает, как мигрировать эталонные правила в существующий проект или настроить их для нового проекта.

## 🎯 Сценарии использования

### Сценарий 1: Новый проект

Если вы создаете новый проект с нуля:

```bash
# 1. Создайте новый Next.js проект
npx create-next-app@latest my-project

# 2. Перейдите в проект
cd my-project

# 3. Скопируйте эталонные правила
cp -r /path/to/project-etalon/.cursor .
cp /path/to/project-etalon/.cursorrules .

# 4. Настройте версию Next.js (см. ниже)

# 5. Установите зависимости
npm install
```

### Сценарий 2: Обновление существующего проекта

Если у вас уже есть проект с правилами, и вы хотите обновить их до эталонной версии:

```bash
# 1. Создайте бэкап текущих правил
mkdir .cursor.backup
cp -r .cursor/* .cursor.backup/
cp .cursorrules .cursorrules.backup

# 2. Скопируйте новые правила
cp -r /path/to/project-etalon/.cursor .
cp /path/to/project-etalon/.cursorrules .

# 3. Настройте версию Next.js (см. ниже)

# 4. Сравните и перенесите проект-специфичные правила из бэкапа
# (если были кастомные правила для вашего проекта)
```

### Сценарий 3: Миграция между версиями Next.js

Если вы обновляете проект с Next.js 14 на Next.js 15 (или 16):

```bash
# 1. Обновите package.json
npm install next@15 react@19 react-dom@19

# 2. Обновите Auth.js (если используется)
npm install next-auth@5  # или auth.js@5

# 3. Измените версию в .cursorrules
# Откройте файл и раскомментируйте секцию Next.js 15

# 4. Обновите rules.json
# Измените путь tech-stack на tech-stack-nextjs15.json

# 5. Запустите проверку
npm run lint
npx tsc --noEmit
npm run build
```

## ⚙️ Пошаговая настройка

### Шаг 1: Определите версию Next.js

Проверьте версию Next.js в вашем проекте:

```bash
cat package.json | grep "next"
```

Результат покажет версию:

- `"next": "^14.x.x"` → используйте конфигурацию Next.js 14
- `"next": "^15.x.x"` → используйте конфигурацию Next.js 15
- `"next": "^16.x.x"` → используйте конфигурацию Next.js 16

### Шаг 2: Настройте .cursorrules

Откройте файл `.cursorrules` и найдите секцию:

```
# ============================================
# STACK - ВЫБЕРИТЕ НУЖНУЮ ВЕРСИЮ
# ============================================
```

**Для Next.js 14:**

```
# --- Next.js 14 + React 18 ---
Stack in my application:
Next.js 14, App Router
React 18
Typescript
Database: PostgreSQL
Global state: Zustand
Libraries:
Shadcn UI,
TailwindCSS,
zod,
React Hook Form,
date-fns,
Auth.js 4

# Удалите или закомментируйте секции для Next.js 15 и 16
```

**Для Next.js 15:**

```
# --- Next.js 15 + React 19 ---
Stack in my application:
Next.js 15, App Router - attention the version is important!,
React 19 - attention the version is important!,
Typescript
Database: PostgreSQL
Global state: Zustand
Libraries:
Shadcn UI,
TailwindCSS,
zod,
React Hook Form,
date-fns,
Auth.js 5 - attention the version is important!

# Удалите или закомментируйте секции для Next.js 14 и 16
```

### Шаг 3: Настройте rules.json

Откройте файл `.cursor/rules.json` и найдите блок с tech-stack:

```json
{
  "name": "tech-stack",
  "path": "rules/tech-stack-nextjs15.json", // ← Измените на нужную версию
  "priority": 100,
  "enabled": true
}
```

Варианты:

- `rules/tech-stack-nextjs14.json` для Next.js 14
- `rules/tech-stack-nextjs15.json` для Next.js 15
- `rules/tech-stack-nextjs16.json` для Next.js 16

### Шаг 4: Проверьте соответствие версий

Убедитесь, что версии совпадают во всех местах:

```bash
# Проверьте package.json
cat package.json | grep -E "(next|react|next-auth)"

# Проверьте .cursorrules
head -60 .cursorrules | grep -A 10 "Stack in my application"

# Проверьте rules.json
cat .cursor/rules.json | grep -A 3 "tech-stack"
```

### Шаг 5: Перенесите проект-специфичные настройки

Если у вас были кастомные правила в старом `.cursorrules` или `.cursor/rules/`, перенесите их:

1. **Для .cursorrules:**

   - Добавьте кастомные секции в конец нового файла
   - Не дублируйте существующие правила

2. **Для .cursor/rules/:**
   - Создайте новый JSON файл, например `project-custom.json`
   - Добавьте ссылку на него в `rules.json` с приоритетом 25

Пример:

```json
{
  "name": "project-custom",
  "path": "rules/project-custom.json",
  "priority": 25,
  "enabled": true
}
```

### Шаг 6: Тестирование

После миграции запустите полную проверку:

```bash
# Форматирование
npm run format

# Линтинг
npm run lint

# TypeScript проверка
npx tsc --noEmit

# Сборка
npm run build
```

Если есть ошибки:

1. Проверьте соответствие версий
2. Убедитесь, что все зависимости установлены
3. Проверьте синтаксис JSON файлов

## 🔄 Обновление эталонных правил

Когда вы добавляете новое правило или улучшаете существующее:

### 1. Обновите эталон

```bash
# Перейдите в эталонный проект
cd /path/to/project-etalon

# Обновите нужный файл
vim .cursor/rules/code-style.json

# Или обновите .cursorrules
vim .cursorrules
```

### 2. Синхронизируйте с другими проектами

```bash
# Для каждого проекта
cd /path/to/project-1
cp /path/to/project-etalon/.cursor/rules/code-style.json .cursor/rules/

# Или скопируйте все правила
cp -r /path/to/project-etalon/.cursor/rules/* .cursor/rules/
```

### 3. Документируйте изменения

Добавьте запись в CHANGELOG или commit message:

```
feat: обновлены правила code-style

- Добавлено правило про использование early returns
- Уточнено правило про TypeScript interfaces vs types
- Обновлена секция про форматирование

Синхронизировано с эталонными правилами v1.1.0
```

## 📋 Чек-лист миграции

Используйте этот чек-лист при миграции правил:

### Перед миграцией

- [ ] Создан бэкап текущих правил (`.cursor.backup/`, `.cursorrules.backup`)
- [ ] Определена текущая версия Next.js в `package.json`
- [ ] Определена версия Auth.js (если используется)
- [ ] Проект успешно собирается (`npm run build`)

### Во время миграции

- [ ] Скопированы новые правила из эталона
- [ ] Выбрана правильная версия Stack в `.cursorrules`
- [ ] Настроен правильный tech-stack в `.cursor/rules.json`
- [ ] Перенесены проект-специфичные правила (если были)
- [ ] Проверен синтаксис всех JSON файлов

### После миграции

- [ ] Перезапущен Cursor IDE
- [ ] Запущено форматирование (`npm run format`)
- [ ] Запущен линтинг (`npm run lint`) - 0 ошибок
- [ ] Запущена TypeScript проверка (`npx tsc --noEmit`) - 0 ошибок
- [ ] Проект успешно собирается (`npm run build`)
- [ ] Протестирована работа AI с новыми правилами
- [ ] Удален бэкап (если все работает)

## 🆘 Устранение проблем при миграции

### Проблема: После миграции проект не собирается

**Возможные причины:**

1. Несоответствие версий в `.cursorrules` и `package.json`
2. Неправильно выбран tech-stack в `rules.json`
3. Отсутствуют зависимости

**Решение:**

```bash
# 1. Проверьте версии
cat package.json | grep -E "(next|react)"
head -60 .cursorrules | grep "Next.js"

# 2. Установите зависимости
npm install

# 3. Если используется Next.js 15, убедитесь что React 19
npm install react@19 react-dom@19

# 4. Очистите кэш и пересоберите
rm -rf .next
npm run build
```

### Проблема: AI не применяет новые правила

**Возможные причины:**

1. Cursor IDE не перезапущен
2. Неправильный синтаксис JSON
3. Правило отключено в `rules.json`

**Решение:**

```bash
# 1. Проверьте синтаксис JSON
cat .cursor/rules.json | jq .

# 2. Проверьте все правила
for file in .cursor/rules/*.json; do
  echo "Проверка $file"
  cat "$file" | jq .
done

# 3. Перезапустите Cursor IDE

# 4. Проверьте, что правила включены
cat .cursor/rules.json | jq '.rules[] | select(.enabled == true)'
```

### Проблема: Конфликт между старыми и новыми правилами

**Решение:**

1. Проверьте бэкап на наличие кастомных правил
2. Перенесите только уникальные правила
3. Не дублируйте правила из эталона

### Проблема: Миграция с Next.js 14 на 15 вызывает ошибки

**Решение:**

```bash
# 1. Обновите все зависимости
npm install next@15 react@19 react-dom@19

# 2. Обновите Auth.js (если используется)
npm install next-auth@5

# 3. Проверьте breaking changes в документации
# https://nextjs.org/docs/upgrading

# 4. Обновите код согласно breaking changes

# 5. Обновите правила до Next.js 15 конфигурации
```

## 📞 Дополнительная помощь

Если возникли проблемы, которые не описаны в этом руководстве:

1. Проверьте `README.md` в папке `.cursor/`
2. Изучите файл `policies.md` для workflow правил
3. Сравните свои правила с эталонными файлами
4. Обратитесь к мейнтейнеру проекта

---

**Версия:** 1.0.0
**Последнее обновление:** 2025-10-26
