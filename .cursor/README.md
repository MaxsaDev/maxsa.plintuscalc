# Эталонные настройки Cursor для Next.js проектов

Этот набор правил является эталонным и содержит самые актуальные и полные правила разработки для всех проектов на Next.js.

## 🎯 Назначение

Эта папка содержит универсальные правила, которые можно копировать в любой проект. Правила поддерживают **множественные версии** Next.js и React, что позволяет легко адаптировать их под конкретный проект.

## 📁 Структура

```
.cursor/
├── README.md                          # Этот файл - инструкция по использованию
├── MIGRATION.md                       # Инструкция по миграции правил в проект
├── settings.json                      # Общие настройки Cursor IDE
├── rules.json                         # Метаданные о правилах и их приоритетах
├── policies.md                        # Обязательные workflow правила для разработки
├── rules/                             # Директория с модульными правилами
│   ├── tech-stack-nextjs14.json      # Stack для Next.js 14 + React 18
│   ├── tech-stack-nextjs15.json      # Stack для Next.js 15 + React 19
│   ├── tech-stack-nextjs16.json      # Stack для Next.js 16 (заготовка)
│   ├── project-structure.json        # Структура проекта
│   ├── code-style.json               # Правила стиля кода
│   ├── react-components.json         # Правила для React компонентов
│   ├── seo-semantic.json             # Правила SEO и семантики
│   ├── imports.json                  # Правила импортов
│   ├── database.json                 # Правила работы с БД
│   ├── ui-styling.json               # Правила UI и стилизации
│   ├── performance.json              # Оптимизация производительности
│   ├── directory-specific.json       # Правила для конкретных директорий
│   ├── file-specific.json            # Правила для конкретных типов файлов
│   ├── communication.json            # Правила коммуникации с AI
│   └── additional.json               # Дополнительные правила
└── templates/                         # Шаблоны компонентов
    └── react-component-templates.json # Готовые шаблоны React компонентов
```

## 🚀 Быстрый старт

### Вариант 1: Копирование в новый проект

```bash
# Перейдите в корень вашего проекта
cd /path/to/your/project

# Скопируйте эталонные правила
cp -r /path/to/project-etalon/.cursor .
cp /path/to/project-etalon/.cursorrules .

# Выберите нужную версию Next.js в файле .cursorrules
# Раскомментируйте нужную секцию Stack и удалите остальные
```

### Вариант 2: Обновление существующего проекта

```bash
# Сделайте бэкап текущих правил
cp -r .cursor .cursor.backup
cp .cursorrules .cursorrules.backup

# Скопируйте новые правила
cp -r /path/to/project-etalon/.cursor .
cp /path/to/project-etalon/.cursorrules .

# Настройте под вашу версию Next.js (см. ниже)
```

## ⚙️ Настройка под конкретный проект

### Шаг 1: Настройка Stack в .cursorrules

Откройте файл `.cursorrules` и:

1. **Найдите секцию "STACK - ВЫБЕРИТЕ НУЖНУЮ ВЕРСИЮ"**
2. **Раскомментируйте** нужную версию:
   - Next.js 14 + React 18 + Auth.js 4
   - Next.js 15 + React 19 + Auth.js 5
   - Next.js 16 + React 19 + Auth.js 5 (будущая)
3. **Удалите или закомментируйте** остальные версии

### Шаг 2: Настройка rules.json

Откройте `.cursor/rules.json` и выберите нужный файл tech-stack:

**Для Next.js 14:**

```json
{
  "name": "tech-stack",
  "path": "rules/tech-stack-nextjs14.json",
  "priority": 100,
  "enabled": true
}
```

**Для Next.js 15:**

```json
{
  "name": "tech-stack",
  "path": "rules/tech-stack-nextjs15.json",
  "priority": 100,
  "enabled": true
}
```

**Для Next.js 16:**

```json
{
  "name": "tech-stack",
  "path": "rules/tech-stack-nextjs16.json",
  "priority": 100,
  "enabled": true
}
```

### Шаг 3: Проверка

После настройки проверьте, что:

- [ ] Выбрана правильная версия Next.js в `.cursorrules`
- [ ] Выбран правильный tech-stack в `.cursor/rules.json`
- [ ] Версия совпадает с версией в `package.json`

## 📚 Как работают правила

### Приоритеты правил

Правила применяются в порядке их приоритета (больше число = выше приоритет):

| Приоритет | Правило            | Описание                                     |
| --------- | ------------------ | -------------------------------------------- |
| 100       | tech-stack         | Технологический стек (версионно-специфичный) |
| 90        | project-structure  | Структура проекта                            |
| 80        | code-style         | Стиль кода                                   |
| 79        | react-components   | React компоненты (forwardRef, displayName)   |
| 78        | seo-semantic       | SEO и семантика                              |
| 75        | imports            | Правила импортов                             |
| 70        | database           | Работа с PostgreSQL                          |
| 60        | ui-styling         | UI и стилизация                              |
| 50        | performance        | Оптимизация производительности               |
| 47        | directory-specific | Специфичные правила для директорий           |
| 45        | file-specific      | Специфичные правила для типов файлов         |
| 40        | communication      | Коммуникация с AI                            |
| 30        | additional         | Дополнительные правила                       |

### Включение/отключение правил

Чтобы отключить правило, измените `enabled` на `false` в `.cursor/rules.json`:

```json
{
  "name": "seo-semantic",
  "path": "rules/seo-semantic.json",
  "priority": 78,
  "enabled": false // ← отключено
}
```

## 🔄 Версионные различия

### Next.js 14 vs 15

| Аспект            | Next.js 14 | Next.js 15         |
| ----------------- | ---------- | ------------------ |
| React             | 18         | 19                 |
| Auth              | Auth.js 4  | Auth.js 5          |
| App Router        | Да         | Да (улучшен)       |
| Server Components | Да         | Да (оптимизирован) |

### Next.js 16 (Будущая версия)

Когда выйдет Next.js 16:

1. Обновите файл `tech-stack-nextjs16.json` с новыми особенностями
2. Скопируйте обновленные эталонные правила в проекты
3. Измените версию в `.cursorrules` и `rules.json`

## 🎨 Шаблоны компонентов

В папке `templates/` находятся готовые шаблоны для:

- **ForwardRef Button** - кнопка с forwardRef и displayName
- **ForwardRef Icon Button** - иконочная кнопка с условным рендерингом
- **Memo Component** - мемоизированный компонент
- **HOC Component** - Higher-Order Component

Подробнее см. в файле `.cursor/templates/react-component-templates.json`

## 📋 Workflow правила

Обязательные правила workflow описаны в файле `.cursor/policies.md`:

1. **План перед реализацией** - всегда согласовывайте план перед кодированием
2. **Диагностика вместо гипотез** - используйте логирование для отладки
3. **Комплексная проверка** - запускайте полную проверку после каждой задачи

## 🔧 Настройка IDE

Файл `.cursor/settings.json` содержит рекомендуемые настройки:

- AI provider: Anthropic Claude
- Format on save: включен
- Tab size: 2 spaces
- Trim trailing whitespace: включен

## 💡 Лучшие практики

1. **Регулярно обновляйте эталон** - когда добавляете новое правило в один проект, добавьте его в эталон
2. **Синхронизируйте проекты** - периодически копируйте обновленный эталон во все проекты
3. **Документируйте изменения** - при изменении правил указывайте причину в комментариях
4. **Тестируйте правила** - убедитесь, что новые правила не конфликтуют с существующими

## 🆘 Устранение проблем

### Проблема: AI не применяет правила

**Решение:**

1. Проверьте, что правило включено в `.cursor/rules.json`
2. Проверьте синтаксис JSON файлов
3. Перезапустите Cursor IDE

### Проблема: Конфликт между правилами из .cursorrules и .cursor/rules/

**Решение:**

- Правила из `.cursor/rules/` имеют более высокий приоритет
- Используйте `.cursorrules` для общих правил
- Используйте `.cursor/rules/` для специфичных и детальных правил

### Проблема: После копирования правил проект не собирается

**Решение:**

1. Проверьте соответствие версий в `.cursorrules`, `rules.json` и `package.json`
2. Убедитесь, что выбрана правильная версия tech-stack
3. Запустите `npm install` для обновления зависимостей

## 📞 Поддержка

Если у вас возникли вопросы или предложения по улучшению эталонных правил, создайте issue или обратитесь к мейнтейнеру проекта.

---

**Версия эталонных правил:** 1.0.0
**Последнее обновление:** 2025-10-26
