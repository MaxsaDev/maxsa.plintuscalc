# Cursor IDE Policies - Обязательные правила workflow

Эти правила описывают **обязательный** рабочий процесс при разработке с использованием Cursor IDE.

## 🎯 Обязательный workflow

### 1. План перед реализацией

**Правило:** Перед каждой задачей необходимо создать и согласовать краткий план.

**Что включает план:**

- Шаги реализации (пронумерованные)
- Ожидаемый результат
- Файлы, которые будут изменены
- Потенциальные риски или сложности

**Пример:**

```
План реализации добавления формы регистрации:

1. Создать схему валидации Zod в schemas/register-schema.ts
2. Создать action для регистрации в actions/auth/register.ts
3. Создать компонент формы в components/auth/register-form.tsx
4. Добавить страницу регистрации в app/(auth)/register/page.tsx
5. Добавить интеграцию с Auth.js

Ожидаемый результат: Работающая форма регистрации с валидацией и сохранением в БД

Файлы: 4 новых файла, возможно изменение в lib/auth.ts

Риски: Необходимо убедиться в правильной хэширровании пароля
```

**Почему это важно:**

- Предотвращает хаотичные изменения
- Позволяет заранее увидеть проблемы
- Экономит время на переделках

### 2. Диагностика вместо гипотез

**Правило:** Если поведение некорректно или неочевидно - НЕ перебирать гипотезы методом проб и ошибок.

**Что делать вместо этого:**

1. **Добавить детальное логирование** в критических точках:

   ```typescript
   // Точки входа
   console.log('[API /register] Получены данные:', { email, timestamp: Date.now() });

   // Расчеты
   console.log('[validatePassword] Проверка пароля:', {
     length: password.length,
     hasUppercase: /[A-Z]/.test(password),
   });

   // Результаты
   console.log('[DB insert] Результат сохранения:', { userId, success: true });
   ```

2. **Собрать фактические данные** - что происходит на самом деле

3. **На основании фактов** - внести исправление

**Важно:**

- Логирование должно быть безопасным (без PII - персональных данных)
- Используйте префиксы для фильтрации: `[ModuleName]`
- После исправления логи можно удалить или обернуть в `if (process.env.DEBUG)`

**Почему это важно:**

- Экономит время на отладку
- Предотвращает введение новых багов "вслепую"
- Дает понимание реального поведения системы

### 3. Комплексная проверка после каждой задачи

**Правило:** После завершения ЛЮБОЙ задачи необходимо выполнить полную проверку локально.

**Команда для проверки:**

```bash
npm run format && clear && npm run lint && npx tsc --noEmit && npm run build
```

**Что проверяется:**

1. `npm run format` - форматирование кода (Prettier)
2. `npm run lint` - линтинг (ESLint)
3. `npx tsc --noEmit` - проверка типов TypeScript
4. `npm run build` - успешная сборка проекта

**Критерий завершения:**

- ✅ **Ноль** ошибок
- ✅ **Ноль** предупреждений
- ✅ Успешная сборка

**Если есть ошибки или предупреждения:**

- ❌ Задача НЕ считается завершенной
- ❌ БЛОКЕР для перехода к следующей задаче
- ✅ Необходимо исправить ВСЕ ошибки до продолжения

**Почему это важно:**

- Предотвращает накопление технического долга
- Гарантирует качество кода
- Позволяет рано обнаружить проблемы

## 🛠️ Технические правила

### Технологический стек

- **Фреймворк:** Next.js (14, 15 или 16 - в зависимости от проекта)
- **UI библиотека:** React (18 или 19 - в зависимости от проекта)
- **Язык:** TypeScript (строгий режим)
- **База данных:** PostgreSQL с чистыми SQL-запросами (через pg)
- **Состояние:** Zustand для глобального состояния
- **UI:** shadcn/ui + TailwindCSS
- **Валидация:** Zod
- **Формы:** React Hook Form
- **Даты:** date-fns
- **Аутентификация:** Auth.js (v4 или v5 - в зависимости от проекта)

### Архитектурные принципы

1. **React Server Components (RSC) по умолчанию**

   - `'use client'` только для UI/браузерных API
   - Минимизация клиентского JavaScript

2. **Server-Side Rendering (SSR)**

   - Предзагрузка данных на сервере
   - Передача данных в клиент вместо дублирующих вызовов

3. **Чистый SQL вместо ORM**
   - Используйте прямые SQL запросы через pg
   - Полный контроль над запросами
   - Оптимизация производительности

## 📝 Стиль кода

### TypeScript

- **Interfaces > Types** - предпочитайте интерфейсы
- **Без enum** - используйте словари/мапы
- **Без any** - всегда типизируйте
- **Именованные импорты** - избегайте default exports
- **Ноль предупреждений линтера** - строго

### Именование

- **Переменные:** осмысленные имена без аббревиатур

  - ✅ `isLoading`, `hasError`, `userProfile`
  - ❌ `ld`, `err`, `prof`

- **Директории:** lowercase-with-dashes

  - ✅ `auth-wizard`, `user-profile`
  - ❌ `AuthWizard`, `user_profile`

- **Функции:** глаголы + существительные
  - ✅ `getUserById`, `validateEmail`, `fetchOrders`
  - ❌ `user`, `email`, `orders`

### Импорты

- **Алиас '@/' для корня проекта** - всегда

  ```typescript
  ✅ import { Button } from '@/components/ui/button';
  ❌ import { Button } from '../../../../components/ui/button';
  ```

- **Порядок импортов:**

  1. Внешние библиотеки (React, Next.js)
  2. Компоненты и утилиты проекта (через '@/')
  3. Типы и интерфейсы
  4. Стили и ресурсы

  С пустой строкой между группами

- **Импорт из React напрямую:**
  ```typescript
  ✅ import { ReactNode, useState, useEffect } from 'react';
  ❌ React.ReactNode, React.useState, React.useEffect
  ```

### Форматирование

- **Ранний возврат** - избегайте глубокой вложенности

  ```typescript
  ✅ if (!user) return null;
  ❌ if (user) { /* много кода */ }
  ```

- **Arrow functions** - везде где возможно

  ```typescript
  ✅ const getUser = async (id: string) => { ... };
  ❌ async function getUser(id: string) { ... }
  ```

- **Declarative JSX** - избегайте императивного кода в рендере

## ⚡ Производительность и данные

### Оптимизация

1. **Мемоизация** - для тяжелых вычислений

   ```typescript
   const expensiveValue = useMemo(() => computeExpensiveValue(a, b), [a, b]);
   ```

2. **Suspense** - для асинхронных компонентов

   ```typescript
   <Suspense fallback={<Loader />}>
     <AsyncComponent />
   </Suspense>
   ```

3. **Dynamic imports** - для некритичных компонентов
   ```typescript
   const HeavyComponent = dynamic(() => import('./HeavyComponent'), {
     loading: () => <Loader />,
   });
   ```

### Логирование

- **Адресное логирование** - с префиксами модулей

  ```typescript
  console.log('[UserService] Fetching user:', userId);
  ```

- **Отключаемое в production**

  ```typescript
  if (process.env.NODE_ENV === 'development') {
    console.log('[Debug] Some info');
  }
  ```

- **Безопасное** - без персональных данных (PII)
  ```typescript
  ✅ console.log('[Auth] Login attempt:', { userId: user.id });
  ❌ console.log('[Auth] Login attempt:', { email: user.email, password: ... });
  ```

## 🎨 Приоритеты по хукам и состоянию

### Иерархия выбора решения

1. **Можно без хуков?** → Не добавляйте их

   ```typescript
   ✅ Чистая функция или RSC
   ❌ Ненужный custom hook
   ```

2. **Можно без useEffect/useState?** → Не используйте их

   ```typescript
   ✅ Derived props, computed values
   ❌ Дублирование данных в state
   ```

3. **Нужно состояние?** → Zustand вместо локального state
   ```typescript
   ✅ Zustand store для shared state
   ❌ useState в каждом компоненте
   ```

### Примеры

**Плохо:**

```typescript
const [fullName, setFullName] = useState('');

useEffect(() => {
  setFullName(`${firstName} ${lastName}`);
}, [firstName, lastName]);
```

**Хорошо:**

```typescript
const fullName = `${firstName} ${lastName}`; // Computed value
```

## 🌍 Языковые требования

### Код

- **Комментарии в коде** - украинский
  ```typescript
  // Перевірка валідності email
  const validateEmail = (email: string) => { ... };
  ```

### UI

- **Все текстовые строки для пользователя** - украинский
  ```typescript
  <Button>Зберегти</Button>
  <Error>Помилка валідації</Error>
  ```

### Коммуникация с AI

- **Ответы AI** - русский (для удобства)
- **Документация** - русский

## ✅ Чек-лист для каждой задачи

Используйте этот чек-лист перед завершением задачи:

### Перед началом

- [ ] Создан и согласован план реализации
- [ ] Понятны все требования
- [ ] Определены файлы для изменения

### Во время работы

- [ ] Следование плану
- [ ] Если поведение неочевидно - добавлено логирование
- [ ] Код следует всем правилам стиля
- [ ] Используются правильные импорты (алиас '@/')
- [ ] Комментарии на украинском
- [ ] UI тексты на украинском

### Перед завершением

- [ ] Запущено `npm run format` - успешно
- [ ] Запущено `npm run lint` - 0 ошибок, 0 предупреждений
- [ ] Запущено `npx tsc --noEmit` - 0 ошибок
- [ ] Запущено `npm run build` - успешная сборка
- [ ] Протестирована функциональность вручную
- [ ] Удалено временное логирование (или обернуто в DEBUG флаги)
- [ ] Задача полностью выполнена согласно плану

### Только после прохождения всех проверок

- [ ] ✅ Задача помечена как завершенная
- [ ] ✅ Можно переходить к следующей задаче

## 🚫 Запреты

### Категорически запрещено:

1. **Пропускать проверки** после завершения задачи
2. **Оставлять ошибки линтера** "на потом"
3. **Использовать any в TypeScript** без веской причины
4. **Игнорировать TypeScript ошибки**
5. **Делать коммиты с ошибками сборки**
6. **Перебирать гипотезы** вместо диагностики
7. **Оставлять закомментированный код** в production
8. **Логировать персональные данные** (пароли, email, телефоны)

## 📊 Метрики качества

Поддерживайте эти метрики на уровне:

- **Linter errors:** 0
- **Linter warnings:** 0
- **TypeScript errors:** 0
- **Build errors:** 0
- **Code coverage:** > 80% (рекомендуется)
- **Bundle size:** минимальный (мониторить)

---

**Следование этим правилам обязательно для всех задач в проекте.**

**Версия:** 1.0.0
**Последнее обновление:** 2025-10-26
