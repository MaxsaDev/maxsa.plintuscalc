# ✅ Чеклист налаштування ESLint + Prettier

> **Швидка перевірка правильності налаштувань**

---

## 📦 1. Встановлені пакети

```bash
npm list --depth=0 | grep -E "(prettier|eslint|typescript)"
```

Повинні бути встановлені:

- [ ] `prettier@^3.6.2`
- [ ] `prettier-plugin-tailwindcss@^0.7.1`
- [ ] `eslint@^9`
- [ ] `eslint-config-next@16.0.0`
- [ ] `eslint-plugin-react@^7.37.5`
- [ ] `eslint-plugin-react-hooks@^7.0.1`
- [ ] `eslint-plugin-jsx-a11y@^6.10.2`
- [ ] `@typescript-eslint/eslint-plugin@^8.46.2`
- [ ] `@typescript-eslint/parser@^8.46.2`

---

## 📁 2. Конфігураційні файли

### Перевірте наявність файлів:

- [ ] `.prettierrc` існує
- [ ] `.prettierignore` існує
- [ ] `eslint.config.mjs` існує (НЕ `.eslintrc.*`)
- [ ] `tailwind.config.ts` існує
- [ ] `.editorconfig` існує (опціонально)
- [ ] `.vscode/settings.json` існує (рекомендовано)

### Перевірте відсутність застарілих файлів:

- [ ] Немає `.eslintrc.js`
- [ ] Немає `.eslintrc.json`
- [ ] Немає `.eslintrc.yml`
- [ ] Немає `.eslintignore` (використовується `globalIgnores` в конфігу)

---

## 🔧 3. Вміст .prettierrc

```bash
cat .prettierrc
```

Має містити:

- [ ] `"plugins": ["prettier-plugin-tailwindcss"]` (КРИТИЧНО!)
- [ ] `"semi": true` або `false` (залежно від переваг)
- [ ] `"singleQuote": true` (рекомендовано)
- [ ] `"printWidth": 100`
- [ ] `"arrowParens": "always"` (для Next.js 16)

---

## ⚙️ 4. Вміст eslint.config.mjs

```bash
head -20 eslint.config.mjs
```

Має містити:

- [ ] `import { defineConfig } from 'eslint/config'`
- [ ] `import nextVitals from 'eslint-config-next/core-web-vitals'`
- [ ] `import nextTs from 'eslint-config-next/typescript'`
- [ ] `...nextVitals, ...nextTs` в конфігурації
- [ ] `'react/react-in-jsx-scope': 'off'` в rules

НЕ має містити:

- [ ] НЕ імпортує окремо `eslint-plugin-react`
- [ ] НЕ імпортує окремо `eslint-plugin-jsx-a11y`
- [ ] НЕ має `plugins: { react: ..., 'jsx-a11y': ... }`

---

## 📝 5. NPM скрипти в package.json

```bash
npm run | grep -E "(format|lint|type)"
```

Має бути:

- [ ] `npm run lint` (запускається)
- [ ] `npm run lint:fix` (запускається)
- [ ] `npm run format` (запускається)
- [ ] `npm run format:check` (запускається)
- [ ] `npm run type-check` (запускається)

---

## 🧪 6. Функціональні тести

### Тест 1: Prettier працює

```bash
npm run format:check
```

**Очікується**: `All matched files use Prettier code style!`

- [ ] ✅ Пройдено

---

### Тест 2: ESLint працює

```bash
npm run lint
```

**Очікується**: Немає виводу (0 помилок)

- [ ] ✅ Пройдено

---

### Тест 3: TypeScript перевірка

```bash
npm run type-check
```

**Очікується**: Немає виводу (0 помилок)

- [ ] ✅ Пройдено

---

### Тест 4: Збірка проекту

```bash
npm run build
```

**Очікується**: `✓ Compiled successfully`

- [ ] ✅ Пройдено

---

### Тест 5: Tailwind класи сортуються

Створіть тестовий файл:

```bash
echo 'export default () => <div className="mt-4 p-2 bg-red-500 flex" />' > test.tsx
npm run format
cat test.tsx
```

**Очікується**: Класи відсортовані: `flex mt-4 bg-red-500 p-2`

```bash
rm test.tsx
```

- [ ] ✅ Пройдено

---

## 🎨 7. VSCode інтеграція

### Розширення встановлені

```bash
code --list-extensions | grep -E "(eslint|prettier|tailwind)"
```

Має бути:

- [ ] `dbaeumer.vscode-eslint`
- [ ] `esbenp.prettier-vscode`
- [ ] `bradlc.vscode-tailwindcss`

### Налаштування VSCode

Перевірте `.vscode/settings.json`:

- [ ] `"editor.formatOnSave": true`
- [ ] `"editor.defaultFormatter": "esbenp.prettier-vscode"`
- [ ] `"editor.codeActionsOnSave"` містить `"source.fixAll.eslint"`

---

### Тест автоформатування

1. Відкрийте будь-який `.tsx` файл
2. Додайте невідформатований код: `const x={a:1,b:2}`
3. Збережіть файл (`Cmd+S` / `Ctrl+S`)
4. Перевірте, що код автоматично відформатувався

- [ ] ✅ Автоформатування працює

---

## 🐛 8. Перевірка на типові помилки

### Помилка "Cannot redefine plugin"

```bash
npm run lint 2>&1 | grep "Cannot redefine"
```

**Очікується**: Немає виводу

- [ ] ✅ Помилки немає

---

### Помилка "React must be in scope"

```bash
npm run lint 2>&1 | grep "react-in-jsx-scope"
```

**Очікується**: Немає виводу

- [ ] ✅ Помилки немає

---

### Prettier сортує Tailwind

Створіть компонент з неправильним порядком класів:

```tsx
<div className="text-center p-4 mt-2 bg-red-500">
```

Збережіть файл. Має стати:

```tsx
<div className="mt-2 bg-red-500 p-4 text-center">
```

- [ ] ✅ Сортування працює

---

## 🚀 9. Продакшн готовність

### Тест повного циклу

```bash
npm run format && npm run lint && npm run type-check && npm run build
```

**Очікується**: Всі команди виконуються без помилок

- [ ] ✅ Проект готовий до продакшну

---

## 📊 10. Фінальна перевірка

```bash
echo "=== Версії пакетів ==="
npm list prettier eslint typescript

echo "\n=== Конфігураційні файли ==="
ls -la | grep -E "(prettier|eslint|tailwind|editorconfig)"

echo "\n=== NPM скрипти ==="
npm run 2>&1 | grep -E "(format|lint|type)"

echo "\n=== Тест ESLint ==="
npm run lint

echo "\n=== Тест Prettier ==="
npm run format:check

echo "\n=== Тест TypeScript ==="
npm run type-check
```

---

## ✅ Підсумок

### Мінімальний набір (обов'язково):

- [x] Всі пакети встановлені
- [x] `.prettierrc` та `eslint.config.mjs` створені
- [x] `npm run lint` працює без помилок
- [x] `npm run format` працює без помилок
- [x] `npm run build` успішно збирає проект

### Рекомендований набір:

- [x] VSCode розширення встановлені
- [x] Автоформатування при збереженні працює
- [x] Tailwind класи сортуються автоматично
- [x] `.editorconfig` створений

### Повний набір (ідеально):

- [x] Всі пункти вище виконані
- [x] Документація прочитана
- [x] Команда ознайомлена з налаштуваннями
- [x] Git hooks налаштовані (опціонально)
- [x] CI/CD інтегрований (опціонально)

---

## 🔄 Періодична перевірка

Рекомендується перевіряти налаштування:

- **Щотижня**: Запускати `npm run lint && npm run format:check`
- **Після оновлення Next.js**: Перевіряти сумісність плагінів
- **При додаванні нового розробника**: Провести по чеклісту VSCode

---

## 🆘 Якщо щось не працює

1. Видаліть `node_modules` та `package-lock.json`
2. Запустіть `npm install`
3. Перезапустіть VSCode
4. Перевірте [troubleshooting.md](./troubleshooting.md)

---

**Дата останньої перевірки**: \_\_\_\_\_\_\_\_\_\_\_\_

**Перевірив**: \_\_\_\_\_\_\_\_\_\_\_\_

**Статус**: ✅ Все працює / ⚠️ Є зауваження / ❌ Потрібне виправлення
